/**
 * Framework-neutral vocabulary shared by Toast renderers.
 *
 * @module
 */

/** Tone options shared by web and CLI Toast renderers. */
export type ToastTone = "neutral" | "success" | "warning" | "danger";
