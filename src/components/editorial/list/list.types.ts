/**
 * Framework-neutral vocabulary shared by List renderers.
 *
 * @module
 */

/** Semantic list form shared by browser and terminal renderers. */
export type ListKind = "unordered" | "ordered" | "task";

/** Vertical rhythm shared by browser and terminal List renderers. */
export type ListSpacing = "tight" | "loose";
