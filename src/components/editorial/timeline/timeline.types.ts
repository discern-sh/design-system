/**
 * Framework-neutral vocabulary shared by Timeline renderers.
 *
 * @module
 */

/** Event status shared by web and CLI Timeline renderers. */
export type TimelineStatus = "complete" | "current" | "upcoming";
