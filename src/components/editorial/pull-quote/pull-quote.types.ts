/**
 * Framework-neutral vocabulary shared by Pull quote renderers.
 *
 * @module
 */

/** Reading-measure alignment shared by web and CLI Pull quote renderers. */
export type PullQuoteAlign = "inline" | "wide";
