/**
 * Framework-neutral vocabulary shared by Prose renderers.
 *
 * @module
 */

/** Reading-measure choices shared by web and CLI Prose renderers. */
export type ProseMeasure = "narrow" | "default" | "wide";
