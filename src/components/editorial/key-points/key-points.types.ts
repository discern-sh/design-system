/**
 * Framework-neutral vocabulary shared by Key points renderers.
 *
 * @module
 */

/** Surface tone shared by web and CLI Key points renderers. */
export type KeyPointsTone = "accent" | "sunken" | "contrast";
