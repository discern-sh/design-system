/**
 * Framework-neutral vocabulary shared by Logo renderers.
 *
 * @module
 */

/** Preset visual sizes shared by web and CLI Logo renderers. */
export type LogoSize = "sm" | "md" | "lg" | "xl";

/** Boundary treatments shared by web and CLI Logo renderers. */
export type LogoTreatment = "plain" | "tile";

/** Intrinsic or square layouts shared by web and CLI Logo renderers. */
export type LogoShape = "natural" | "square";
