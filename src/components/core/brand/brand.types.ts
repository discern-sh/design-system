/**
 * Framework-neutral vocabulary shared by Brand renderers.
 *
 * @module
 */

/** Name typefaces shared by web and CLI Brand renderers. */
export type BrandTypeface = "inherit" | "ui" | "display" | "mono";

/** Preset visual sizes shared by web and CLI Brand renderers. */
export type BrandSize = "sm" | "md" | "lg";
