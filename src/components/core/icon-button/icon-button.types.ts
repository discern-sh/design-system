/**
 * Framework-neutral vocabulary shared by Icon button renderers.
 *
 * @module
 */

/** Visual treatments shared by web and CLI Icon button renderers. */
export type IconButtonVariant = "quiet" | "outline";

/** Preset sizes shared by web and CLI Icon button renderers. */
export type IconButtonSize = "sm" | "md" | "lg";
