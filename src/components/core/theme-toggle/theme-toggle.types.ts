/**
 * Framework-neutral vocabulary shared by Theme toggle renderers.
 *
 * @module
 */

/** Site theme roles shared by web and CLI Theme toggle renderers. */
export type ThemeToggleTheme = "light" | "dark";

/** Visual treatments shared by web and CLI Theme toggle renderers. */
export type ThemeToggleVariant = "outlined" | "quiet";
