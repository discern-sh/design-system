/**
 * Framework-neutral vocabulary shared by Button renderers.
 *
 * @module
 */

/** Variant options shared by web and CLI Button renderers. */
export type ButtonVariant = "primary" | "secondary" | "ghost" | "danger";

/** Size options shared by web and CLI Button renderers. */
export type ButtonSize = "sm" | "md" | "lg";
