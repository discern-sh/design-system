/**
 * Framework-neutral vocabulary shared by Section renderers.
 *
 * @module
 */

/** Surface roles shared by web and CLI Section renderers. */
export type SectionSurface = "canvas" | "surface" | "sunken";

/** Vertical rhythm presets shared by web and CLI Section renderers. */
export type SectionSpacing = "none" | "sm" | "md" | "lg";
