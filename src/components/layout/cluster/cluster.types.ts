/**
 * Framework-neutral vocabulary shared by Cluster renderers.
 *
 * @module
 */

/** Cross-axis alignments shared by web and CLI Cluster renderers. */
export type ClusterAlign = "start" | "center" | "end" | "baseline" | "stretch";

/** Main-axis distributions shared by web and CLI Cluster renderers. */
export type ClusterJustify = "start" | "center" | "end" | "between";
