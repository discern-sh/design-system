/**
 * Framework-neutral vocabulary shared by Container renderers.
 *
 * @module
 */

/** Named readable widths shared by web and CLI Container renderers. */
export type ContainerSize = "measure" | "sm" | "md" | "lg" | "full";
