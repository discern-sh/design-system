/** Framework-neutral vocabulary shared by Hero block renderers. @module */

/** Content layouts shared by web and CLI Hero block renderers. */
export type HeroBlockLayout = "split" | "centered" | "showcase";

/** Surface treatments shared by web and CLI Hero block renderers. */
export type HeroBlockSurface = "canvas" | "sunken" | "accent" | "atmospheric";
