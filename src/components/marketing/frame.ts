/**
 * Framework-neutral content-frame vocabulary shared by Marketing blocks that
 * centre their own content.
 *
 * @module
 */

/**
 * Content frame a Marketing block lays out against: the editorial page measure
 * (`--discern-page-max`) or the wider campaign frame (`--discern-page-max-wide`)
 * that campaign chrome shares.
 */
export type MarketingFrame = "standard" | "wide";
