/** Framework-neutral vocabulary shared by CTA band renderers. @module */

/** Surface tones shared by web and CLI CTA band renderers. */
export type CtaBandTone = "accent" | "sunken" | "contrast";

/** Content alignments shared by web and CLI CTA band renderers. */
export type CtaBandAlign = "center" | "split";
