/**
 * Framework-neutral vocabulary shared by Stat renderers.
 *
 * @module
 */

/** Context directions shared by web and CLI Stat renderers. */
export type StatTrend = "positive" | "negative" | "neutral";
