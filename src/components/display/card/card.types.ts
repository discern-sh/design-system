/**
 * Framework-neutral vocabulary shared by Card renderers.
 *
 * @module
 */

/** Surface textures shared by web and CLI Card renderers. */
export type CardTexture = "plain" | "dots";

/** Interior spacing presets shared by web and CLI Card renderers. */
export type CardPadding = "none" | "sm" | "md" | "lg";
