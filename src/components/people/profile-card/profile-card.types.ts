/**
 * Framework-neutral vocabulary shared by Profile card renderers.
 *
 * @module
 */

/** Layout shared by web and CLI Profile card renderers. */
export type ProfileCardLayout = "portrait" | "landscape";
