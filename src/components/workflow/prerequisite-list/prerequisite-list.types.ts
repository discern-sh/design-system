/**
 * Framework-neutral vocabulary shared by Prerequisite list renderers.
 *
 * @module
 */

/** Presentation state shared by web and CLI prerequisite items. */
export type PrerequisiteState = "required" | "satisfied" | "unresolved";
