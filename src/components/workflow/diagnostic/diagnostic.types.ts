/**
 * Framework-neutral vocabulary shared by Diagnostic renderers.
 *
 * @module
 */

/** Severity states shared by web and CLI Diagnostic renderers. */
export type DiagnosticSeverity = "failure" | "attention";
