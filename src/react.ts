/**
 * Optional React adapters for every design-system component, plus their
 * public prop types. This is the only entrypoint that resolves React;
 * consumers rendering static HTML import it beside `react-dom/server`.
 *
 * @module
 */
export * from "./generated/react.ts";
