import { loadComponentSources } from "../../scripts/generate.ts";

// Component discovery now rejects an absent or inconsistent stance before the
// zero ceiling is measured. The metric remains as a trunk-held second line of
// defence because discern Standards cannot be deleted from a feature branch.
await loadComponentSources();
console.log("DISCERN_METRIC cli_pending 0");
