# Runtime emitter

The Emitter in [`runtime.ts`](../../src/runtime.ts): Selection resolution
through the Registry, deterministic output ordering, the Manifest schema
([`manifest.ts`](../../src/manifest.ts)), Optional Assets, and the cross-runtime
`node:fs/promises` write path.

_This subtree is not yet written — filling it is tracked in
[`discern/TODO.md`](../../discern/TODO.md). Until then, start from the
[orientation docs](../00-orientation/)._
