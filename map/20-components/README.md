# Components

The 109 Component folders under [`src/components/`](../../src/components/) cover anatomy (CSS, implementation, Metadata, examples, `mod.ts`), the twelve Groups, Owned Classes, and the authoring rules for adding or changing a Component. Metadata can enrol a Component in Catalogue purpose collections and state when to use or avoid it. Examples can expose stable named states. Component variants carry semantic differences: Theme toggle offers outlined and quiet treatments, Table of contents separates numbered sections from unnumbered nested entries, and Prerequisite list separates static requirements from live verification states. The Workflow Group includes command-and-path primitives, procedure and recovery, result and diagnostic, Artifact, and task-page orientation and handoff families. End-of-page next actions remain a named Branch choice state and source-backed Catalogue recipe rather than a separate Component.

A rendered terminal stance adds `<slug>.cli.ts` to the Component folder. The module exports a pure default renderer, `<Pascal>CliProps`, and deterministic `cliExamples`; Metadata is the authority that enrols those exports into `./cli`. When web and terminal forms share named vocabulary, a framework-neutral sibling `*.types.ts` module owns it so the CLI graph never resolves React. Core, Display, and Layout have complete stance coverage: 21 rendered Components join the Badge exemplar and Theme switcher records the sole exemption. Their exact-frame tests cover width and capability degradation, with Divider selecting the package-owned triangle treatments and the five Layout renderers delegating composition to the CLI foundation.

_This subtree is not yet written — filling it is tracked in [`discern/TODO.md`](../../discern/TODO.md). Until then, start from the [orientation docs](../00-orientation/)._
