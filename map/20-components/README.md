# Components

The 121 Component folders under [`src/components/`](../../src/components/) cover anatomy (CSS, implementation, Metadata, examples, `mod.ts`, and a CLI renderer when the required stance is rendered), the thirteen Groups, Owned Classes, and the authoring rules for adding or changing a Component. Metadata can enrol a Component in Catalogue purpose collections, state when to use or avoid it, and must declare whether its CLI stance is rendered or intentionally exempt with a reason. Examples can expose stable named states. Component variants carry semantic differences: Theme toggle offers outlined and quiet treatments, Table of contents separates numbered sections from unnumbered nested entries, and Prerequisite list separates static requirements from live verification states. End-of-page next actions remain a named Branch choice state and source-backed Catalogue recipe rather than a separate Component.

Theme toggle and Theme switcher serve different user goals. Theme toggle is the compact persistent control for an immediate light/dark comfort adjustment; its consumer passes the resolved theme and owns the policy that stores an override or returns to the system preference. Theme switcher belongs in settings and deterministic inspection surfaces where System, Light, and Dark are deliberate visible choices. The Catalogue uses Theme switcher because inspecting each mode is part of its job.

## Marketing foundations

Marketing section and Marketing intro are the opt-in campaign-page foundation. Marketing section owns standard or wide frames, standard or spacious rhythm, and semantic surface scopes. Its contrast scope remaps ink, surface, and border roles together, keeping ordinary descendants readable on a stable dark chapter in either Theme. Marketing intro owns the recurring eyebrow, heading, and standfirst hierarchy at standard or editorial scale.

Existing Components carry the reusable launch-page treatments as variants, not replacements. Site header's campaign treatment widens and strengthens the translucent masthead. Hero block's showcase layout and atmospheric surface create publication-scale copy above wide visual evidence. Logo cloud's strip loosens the provider line. An item with `markMask` retains its supplied colour artwork in light Theme, then replaces that artwork completely with a neutral silhouette in dark Theme; an item without a mask keeps its supplied artwork in both. Window, Terminal, and Code listing keep their standard frames while offering larger showcase treatments; Window and Terminal accept trailing chrome, and Terminal can carry a contextual footer. Global Layout, Display, and Token defaults remain unchanged, while provider data, narrative content, semantic diagrams, and identity-specific artwork remain consumer-owned. [ADR-0006](../_adr/0006-homepage-treatments-ship-as-variants.md) records the opt-in variant boundary.

## Artwork grounds

The Artwork Group owns reusable decorative material rather than consumer narrative. Ground is the shared absolute plane: it maps public presence and accent controls onto one optical scale, exposes ambient or still motion, hides the entire layer from assistive technology, resolves a complete still under reduced motion, and disappears in forced-colour modes. The Theme-owned `--discern-ground-theme-gain` Token balances apparent opacity across light and dark canvases.

Eight concrete Grounds depend on that foundation without depending on one another. Survey is an exact tileable triangular ruling; Approach gathers nested triangles at a right-hand station; Fold is an authored faceted field; Aperture admits three unequal beams; Impression repeats a configurable glyph through a pointer-following aperture and defaults to `◐`; Envelope describes curves using only straight chords; Cleave leaves two oversized surfaces around one median; Resonance joins two wave fields along a narrow shared contour. Each is an individually selectable Component with no terminal equivalent.

Hero block's optional `ground` slot places any decorative node behind its complete foreground composition. Marketing does not import Artwork, so selecting Hero block alone adds no Ground CSS and each Ground also works behind consumer-owned sections. Bespoke identity art, logos, product evidence, semantic diagrams, and page-specific narrative compositions stay with the consumer. [ADR-0008](../_adr/0008-package-owns-reusable-decorative-artwork.md) records that qualified ownership boundary.

## Workflow and Agents

The Workflow Group is the operational vocabulary: command and path primitives, procedures and recovery, results and diagnostics, artifacts, standards, task orientation, and handoffs. All 23 Workflow Components have pure terminal renderers beside their web implementations. Commands keep run context and proof visible; Artifact tree uses capability-aware connectors; Procedure and Procedure step pass semantic statuses and phases to the package triangle stepper; Standard meter passes scale values to the package progress primitive rather than owning another triangle cycle. Activity log presents long-running work as one calm frame — pinned narration-toned results above a bounded streamed tail with an in-progress partial line — and its terminal form accepts the interactive activity-log frame state directly ([ADR-0016](../_adr/0016-stream-activity-through-a-component-backed-log-frame.md)).

The Agents Group is the identity and parallel-work vocabulary. All seven Agents Components have terminal renderers: initials chips and mentions, persona lockups, Fleet's width-adaptive status table, boxed Receipt proofs, ordered Transcript turns, and Worklog's status feed. Fleet and Worklog accept beacon phases as state and render one frame through the package activity-beacon primitive; neither reads a clock nor advances itself.

Whenever web props carry a closed semantic vocabulary, such as agent status, file disposition, receipt state, or standard direction, that vocabulary lives in a framework-neutral `<slug>.types.ts` module imported by both renderers. CLI-specific textual item shapes remain plain strings, so the generated `./cli` graph cannot acquire React through a `ReactNode` prop type. Exact-frame coverage for these groups lives in [`tests/cli/`](../../tests/cli/) and exercises narrow, standard, and wide layouts across truecolour, ANSI 256, ANSI 16, no-colour, Unicode, and ASCII capabilities.

A rendered terminal stance adds `<slug>.cli.ts` to the Component folder. The module exports a pure default renderer, `<Pascal>CliProps`, and deterministic `cliExamples`; Metadata is the authority that enrols those exports into `./cli`. When web and terminal forms share named vocabulary, a framework-neutral sibling `*.types.ts` module owns it so the CLI graph never resolves React. Core, Display, and Layout have complete stance coverage: 21 rendered Components join the Badge exemplar and Theme switcher records the sole exemption. Their exact-frame tests cover width and capability degradation, with Divider selecting the package-owned triangle treatments and the five Layout renderers delegating composition to the CLI foundation.

Feedback, Forms, Navigation, and Marketing have complete CLI stance coverage. Components own pure terminal renderers unless Metadata records a concrete mismatch: Feedback's Hover card and Tooltip depend on browser hover/focus disclosure; Marketing's Feature bento, Site header, Site footer, and Split feature depend on browser chrome or media composition; and Marketing section and Marketing intro own browser-only page framing and responsive typography. Forms expose idle, active, filled, validation-error, disabled, submitted, and cancelled frames; their renderer props directly extend the framework-neutral Wave 1 state types wherever one applies.

Component Metadata also owns each terminal stance. Editorial's twelve Components and People's six Components have pure CLI renderers; Docs has six pure renderers and three explicit browser-only exemptions. Shared web-and-terminal vocabulary lives in React-free sibling `*.types.ts` modules, while terminal implementations remain colocated as `*.cli.ts`. Copy button, Search palette, and Skip link are exempt because clipboard feedback, modal input and focus management, and browser focus-bypass navigation require interaction models that deterministic terminal output does not provide.

_This subtree is not yet written — filling it is tracked in [`discern/TODO.md`](../../discern/TODO.md). Until then, start from the [orientation docs](../00-orientation/)._
