# Components

The 137 Component folders under [`src/components/`](../../src/components/) cover anatomy (CSS, implementation, Metadata, examples, `mod.ts`, and a CLI renderer when the required stance is rendered), the thirteen Groups, Owned Classes, and the authoring rules for adding or changing a Component. Metadata can enrol a Component in Catalogue purpose collections, state when to use or avoid it, and must declare whether its CLI stance is rendered or intentionally exempt with a reason. Examples can expose stable named states. Component variants carry semantic differences: Theme toggle offers outlined and quiet treatments, Table of contents separates numbered sections from unnumbered nested entries, and Prerequisite list separates static requirements from live verification states. End-of-page next actions remain a named Branch choice state and source-backed Catalogue recipe rather than a separate Component.

Paragraph is one semantic unit of body prose. Its React adapter renders a native `<p>` around ordinary phrasing children; its terminal renderer accepts the package-owned [`SemanticInlineContent`](../../src/cli/semantic-inline.ts), an optional Theme, and a capability-bounded maximum measure, then returns wrapped styled lines with no leading or trailing blank-line boundary. Prose is the multi-block reading context, not another parser: its original terminal `text` contract remains exact, while its semantic path composes Paragraph children and opaque `CliBlock` children at the selected reading measure through `composeCliBlocks`. Lead and drop-cap treatment applies to that path only when the first child is a wholly plain string Paragraph; a rich-inline or structural first child retains its own styling untouched.

Heading owns document outline hierarchy rather than styled lead text. Its browser form remains a native `h1`–`h6` around ordinary phrasing content. The terminal default retains the compact `text`/`accent` frame and one-line truncation; the additive semantic `content` path can select `overflow: "wrap"` to preserve complete inline styling, links, soft and hard breaks, and accents, with every continuation aligned beneath the content after its structural prefix. The styled-Unicode `document` treatment gives H1 a motif-led title and full heavy underline, H2 a full light underline, H3 a quiet end-capped inline rule, and H4–H6 the established strong, emphasis, and annotation roles; Markdown selects this same authority, while no-colour and ASCII modes retain source markers. `leadingBlankLines: 0` lets an enclosing composer own the boundary. The inline content-model ownership and parser-adaptation boundary are recorded in [ADR-0019](../_adr/0019-own-semantic-inline-content.md).

List is the neutral document list rather than an operational or editorial substitute. It renders native `ul`/`ol`/`li` structure, supports explicit ordered starts, tight or loose rhythm, recursively mixed list kinds, rich item phrasing, continuation blocks, and disabled read-only task markers whose checked state remains distinct from an interactive Checkbox. Procedure still owns executable sequences, Prerequisite list owns readiness states, and Key points owns a curated summary.

Blockquote renders an ordinary native `<blockquote>` around one or more semantic blocks and adds no quotation marks, attribution, citation, title, or tone. Pull quote remains the attributed display treatment and Callout remains the highlighted semantic aside. Its terminal rail recursively narrows child Components and repeats on intentional blank lines, so Lists, Paragraphs, Headings, Code blocks, and nested quotations retain their own output rather than flattening into quote text.

Code block is the literal, non-line-numbered `pre > code` primitive. It retains browser source whitespace and exposes optional language/info through namespaced data hooks without bundling a highlighter. Browser output keeps ASCII as ordinary text and fixes each non-ASCII grapheme to the terminal width returned by the shared projection authority, so a proportional fallback glyph cannot move a box edge while the complete source remains the element's text content. Its terminal renderer uses a rounded specimen frame with language and info embedded as `ts · module`, makes controls visible, expands tabs to four-cell stops measured from the start of each source line, preserves logical blank lines, and either wraps every grapheme losslessly behind an explicit continuation cell or expands the complete frame to preserve source width. Code listing remains the captioned, referenceable, line-numbered editorial figure; Terminal and Raw output retain their transcript and workflow-output meanings.

Table owns ordinary relational data; Comparison table remains the curated two-option marketing narrative. Browser consumers author native `thead`, `tbody`, `th`, and `td` structure inside Table's overflow wrapper, including rich phrasing and genuinely empty cells. `tableCellAlignmentProps()` maps optional logical start/centre/end alignment onto Table's namespaced cell hook, so generated consumers and source-driven adapters use the same Table-owned CSS rather than inventing local alignment rules. The terminal default remains the exact compact one-line string grid. The opt-in `layout: "responsive"` contract accepts semantic inline headers and cells, preserves GFM-compatible start/centre/end alignment, expands rows to fit wrapped content, normalises missing trailing cells to explicit empties, and rejects surplus unlabelled cells. When a framed grid cannot leave four content cells per column it projects each row as labelled records instead of truncating, throwing for narrowness, or concatenating fields; zero-column, header-only, empty-header, and many-column inputs have deterministic forms.

Footnotes owns end-note definitions and their return relationship; an ordinary List does not imply bidirectional references. Browser definitions retain native ordered-list structure, rich React blocks, unique stable ids, and one or more ordered return links. The terminal renderer preserves its original anonymous plain-string item frames, while additive linked-text, semantic-inline, and multi-block items require stable ids, retain paragraphs, Lists, Blockquotes, and Code blocks as separate blocks, and render every repeated return target through the package hyperlink authority with visible no-colour and ASCII fallbacks. Duplicate ids reject; rich content or linked returns without an id reject; resolving a reference whose definition is absent remains the owning document renderer's whole-document responsibility.

Markdown is the source-to-document Component. `Markdown` in `./react` accepts untrusted `source`, an optional Prose `measure`, optional explicit diagram resources, and safe wrapper attributes; it emits native headings, paragraphs, lists, blockquotes, code, tables, images, links, footnote navigation, and admitted live Diagrams by composing the real Components and semantic-inline foundation. Its fenced and indented code inherits Code block's browser cell projection, so source-aligned Unicode diagrams remain aligned independently of the selected monospace font's glyph coverage. `renderMarkdownCli` in `./cli` accepts the same source and resources plus explicit Theme, an optional package-validated terminal motif, capability-bounded `maxWidth`, and an optional description-only diagram preference; it dispatches every block through the public renderers, selects Divider's unmarked plain rule for thematic breaks, recursively narrows structural children, and joins only through the shared block-rhythm authority. `renderDiagramMarkdownImage()` is the standard-syntax serializer for a registered source/spec pair; no custom Markdown grammar exists. A valid unused resource is allowed for corpus-level sharing. Empty source produces no browser wrapper and an empty terminal string.

Chart is the quantitative reference visual: one generated `ChartSpec` union (currently the `bar` kind with grouped and proportion variants) rendered from validated, conformance-proven scenes. The React adapter accepts only the semantic spec, an SVG identity, and a composition class inside the same focusable scrollable viewport treatment as Diagram; scales, ticks, locale-free number formatting, series colour, and accessibility wording stay package-owned. Series identity rides the six fixed `--discern-color-series-1..6` Tokens paired with per-slot marker and fill glyphs, and `DataFigure` legend items additively accept those series tones — browser swatches take distinct marker shapes, terminal legends the paired glyphs — fed by the `./chart` entrypoint's `chartSeriesLegend` rather than an auto-injected legend. Chart's terminal Component tries the bar kind's `exact`-tier frame (every authored value printed beside its eighth-block bar) and renders the universal description with a real aligned data table on any typed decline; the bar orientation hint shapes only SVG and React output. Stat and Meter keep single headline values; Diagram keeps identity and topology.

Diagram is the meaningful reference visual, not a caller drawing surface. Its one generated `DiagramSpec` union covers five reader tasks: trace a process with `flow`, locate bounded systems with `architecture`, revisit repetition with `cycle`, follow interaction chronology with `sequence`, and compare dated spans with `timeline`. The React adapter accepts only that semantic spec, an SVG identity, and an SVG composition class; validation, layout, accessibility wording, and scene geometry remain package-owned. A focusable wrapper provides local horizontal scrolling without shrinking or clipping the intrinsic SVG. The SVG contains accessible title and complete structural description but no visible title, caption, source, or legend: compose it as `visual={<Diagram spec={spec} />}` inside DataFigure when those editorial facts are required. The duration-bar `timeline` kind remains distinct from the Editorial Timeline Component's narrative entries. Semantically disposable Backdrops and bespoke consumer-owned illustrative or interactive explainers remain separate boundaries. Diagram's terminal Component enhances bounded flow, cycle, and sequence specs opportunistically; architecture and timeline intentionally use the universal description, which is every kind's correctness floor.

The fixed dialect is CommonMark plus GFM tables, task lists, strikethrough and literal autolinks, the five GitHub alert markers, and footnotes. One pinned mdast/micromark parse feeds a private package-owned tree before either projection: no parser token becomes public and no React import enters the CLI graph. GitHub-compatible duplicate-safe heading ids, ordered starts, table alignment, repeated footnote relationships, reference resolution, code info strings, raw-HTML literalisation, comment omission, destination validation, visible source controls, and whole-document byte/node/depth limits are settled before presentation. The exact parser stack, licence evidence, AST-containment rule, alert tones, safety posture, and limits are recorded in [ADR-0020](../_adr/0020-parse-markdown-through-mdast.md). [ADR-0029](../_adr/0029-upgrade-markdown-images-through-explicit-diagram-resources.md) records the separate post-parse boundary that can upgrade one isolated ordinary image through an explicit source-to-spec resource without extending that dialect.

Theme toggle and Theme switcher serve different user goals. Theme toggle is the compact persistent control for an immediate light/dark comfort adjustment; its consumer passes the resolved theme and owns the policy that stores an override or returns to the system preference. Theme switcher belongs in settings and deterministic inspection surfaces where System, Light, and Dark are deliberate visible choices. The Catalogue uses Theme switcher because inspecting each mode is part of its job.

## Marketing foundations

Marketing section and Marketing intro are the opt-in campaign-page foundation. Marketing section owns standard or wide frames, standard or spacious rhythm, and semantic surface scopes. Its contrast scope remaps ink, surface, and border roles together, keeping ordinary descendants readable on a stable dark chapter in either Theme. Marketing intro owns the recurring eyebrow, heading, and standfirst hierarchy at standard or editorial scale.

The complementary reading-first family composes those foundations instead of changing the established launch-page Components. Marketing stage gives conceptual, atmospheric, and concise evidential visuals one quiet frame. Editorial hero reserves the broadest type for a page opening; Narrative chapter owns a substantial text-first explanation; Journey overview compresses a process into audience-facing moments; Outcome spotlight makes one result dominant; Voice break changes pace through one concise quotation; and Closing statement resolves a page around one proposition and action. Their complete Catalogue recipe is the authority for page rhythm: explanation alternates with relief, compression, evidence, human voice, and action rather than mounting an equally dense visual beside every passage.

These Components deliberately leave product concepts, claims, diagrams, customer evidence, and artwork with the consumer. A visual earns its place when it compresses, demonstrates, proves, or creates relief; otherwise the text-first state is complete without it. The family is browser-only because its contract is page-scale proportion and cognitive pacing, not another terminal projection of the same content.

Existing Components carry the reusable launch-page treatments as variants, not replacements. Site header's campaign treatment widens and strengthens the translucent masthead. Hero block's showcase layout and atmospheric surface create publication-scale copy above wide visual evidence. Logo cloud's strip loosens the provider line. An item with `markMask` retains its supplied colour artwork in light Theme, then replaces that artwork completely with a neutral silhouette in dark Theme; an item without a mask keeps its supplied artwork in both. Window, Terminal, and Code listing keep their standard frames while offering larger showcase treatments; Window and Terminal accept trailing chrome, and Terminal can carry a contextual footer. Global Layout, Display, and Token defaults remain unchanged, while provider data, narrative content, semantic diagrams, and identity-specific artwork remain consumer-owned. [ADR-0006](../_adr/0006-homepage-treatments-ship-as-variants.md) records the opt-in variant boundary.

## Artwork backdrops

The Artwork Group owns reusable decorative material rather than consumer narrative. Backdrop is the shared absolute plane: it maps public presence and accent controls onto one optical scale, exposes ambient or still motion, hides the entire layer from assistive technology, resolves a complete still under reduced motion, and disappears in forced-colour modes. The Theme-owned `--discern-backdrop-theme-gain` Token balances apparent opacity across light and dark canvases.

Ten concrete Backdrops depend on that foundation without depending on one another. Survey is an exact tileable triangular ruling; Approach gathers nested triangles at a right-hand station; Fold is an authored faceted field; Aperture admits three unequal beams; Impression repeats a configurable glyph through a pointer-following aperture and defaults to `◐`; Envelope describes curves using only straight chords; Cleave leaves two oversized surfaces around one median; Tiling traces a route through reconfiguring Truchet arcs; Compression preserves a measured ribbon while its centre folds; and Harmonic gathers fine grains across changing nodal modes. Each is an individually selectable Component with no terminal equivalent.

Hero block's optional `backdrop` slot places any decorative node behind its complete foreground composition. Marketing does not import Artwork, so selecting Hero block alone adds no Backdrop CSS and each Backdrop also works behind consumer-owned sections. Bespoke identity art, logos, product evidence, semantic diagrams, and page-specific narrative compositions stay with the consumer. [ADR-0008](../_adr/0008-package-owns-reusable-decorative-artwork.md) records that qualified ownership boundary.

## Workflow and Agents

The Workflow Group is the operational vocabulary: command and path primitives, procedures and recovery, results and diagnostics, artifacts, standards, task orientation, and handoffs. All 23 Workflow Components have pure terminal renderers beside their web implementations. Commands keep run context and proof visible; Artifact tree uses capability-aware connectors; Procedure and Procedure step pass semantic statuses and phases to the package motif stepper; Standard meter passes scale values to the package progress primitive rather than owning another glyph cycle. Activity log presents long-running work as one calm frame — pinned narration-toned results above a bounded, indented streamed tail whose first row attaches through an angled connector, plus an in-progress partial line — and its terminal form accepts the interactive activity-log frame state directly ([ADR-0016](../_adr/0016-stream-activity-through-a-component-backed-log-frame.md)).

The Agents Group is the identity and parallel-work vocabulary. All seven Agents Components have terminal renderers: initials chips and mentions, persona lockups, Fleet's width-adaptive status table, boxed Receipt proofs whose title carries the pass/fail glyph once, ordered Transcript turns, and Worklog's status feed. Fleet and Worklog accept beacon phases as state and render one frame through the package activity-beacon primitive; neither reads a clock nor advances itself.

Whenever web props carry a closed semantic vocabulary, such as agent status, file disposition, receipt state, or standard direction, that vocabulary lives in a framework-neutral `<slug>.types.ts` module imported by both renderers. CLI-specific textual item shapes remain plain strings, so the generated `./cli` graph cannot acquire React through a `ReactNode` prop type. Exact-frame coverage for these groups lives in [`tests/cli/`](../../tests/cli/) and exercises narrow, standard, and wide layouts across truecolour, ANSI 256, ANSI 16, no-colour, Unicode, and ASCII capabilities.

A rendered terminal stance adds `<slug>.cli.ts` to the Component folder. The module exports a pure default renderer, `<Pascal>CliProps`, and deterministic `cliExamples`; Metadata is the authority that enrols those exports into `./cli`. When web and terminal forms share named vocabulary, a framework-neutral sibling `*.types.ts` module owns it so the CLI graph never resolves React. Core, Display, and Layout have complete stance coverage: 22 rendered Components join the Badge exemplar and Theme switcher records the sole exemption. Their exact-frame tests cover width and capability degradation, with Divider selecting the package-owned motif treatments and the six Layout renderers delegating composition to the CLI foundation. Masonry's terminal form wraps each block at the responsive column measure and packs it into the currently shortest column.

Feedback, Forms, Navigation, and Marketing have complete CLI stance coverage. Components own pure terminal renderers unless Metadata records a concrete mismatch: Feedback's Hover card and Tooltip depend on browser hover/focus disclosure; Marketing's Feature bento owns a strict responsive size-and-media matrix, Site header and Site footer own browser chrome, and Split feature owns media composition; Marketing section and Marketing intro own browser-only page framing and responsive typography. The reading-first Marketing family is also explicitly exempt because its distinguishing contract is browser page rhythm, visual proportion, and cognitive pacing; each folder records the narrower terminal mismatch for its own composition. Forms expose idle, active, filled, validation-error, disabled, submitted, and cancelled frames; their renderer props directly extend the framework-neutral Wave 1 state types wherever one applies.

Component Metadata also owns each terminal stance. Editorial's eighteen Components and People's six Components have pure CLI renderers; Docs has six pure renderers and three explicit browser-only exemptions. Shared web-and-terminal vocabulary lives in React-free sibling `*.types.ts` modules, while terminal implementations remain colocated as `*.cli.ts`. Copy button, Search palette, and Skip link are exempt because clipboard feedback, modal input and focus management, and browser focus-bypass navigation require interaction models that deterministic terminal output does not provide.
