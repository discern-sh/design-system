# Codegen

How [`generate.ts`](../../scripts/generate.ts) turns Component Metadata and package assets into the generated surfaces in [`src/generated/`](../../src/generated/) and `styleguide/generated/` — the Registry, React surface, base styles, asset tables, and example registry.

_This subtree is not yet written — filling it is tracked in [`discern/TODO.md`](../../discern/TODO.md). Until then, start from the [orientation docs](../00-orientation/)._
