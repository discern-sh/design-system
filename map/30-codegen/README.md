# Codegen

How [`generate.ts`](../../scripts/generate.ts) turns Component Metadata and package assets into the committed Registry, React surface, base styles, and asset tables under [`src/generated/`](../../src/generated/). The Catalogue's ignored example registry is a build output: [`build.ts`](../../scripts/build.ts) regenerates it under `catalogue/generated/` before bundling or verification consumes it.

_This subtree is not yet written — filling it is tracked in [`discern/TODO.md`](../../discern/TODO.md). Until then, start from the [orientation docs](../00-orientation/)._
