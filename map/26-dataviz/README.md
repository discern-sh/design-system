# Dataviz

The chart subsystem owns quantitative, documentation-scale charts between author data and presentation: typed `ChartSpec` data validated and laid onto scales, a closed projection-neutral `ChartScene`, and a lossless structural description. It is the second client of the kind-family codegen — a sibling of the diagram family, split by subject: diagrams own identity and topology, charts own quantities on scales. It is also the second client of the shared Markdown image-promotion resolver: [ADR-0031](../_adr/0031-promote-markdown-images-through-one-resource-family.md) records why chart resources ride the one resource-family core rather than a parallel chart-side copy. [ADR-0030](../_adr/0030-own-charts-as-a-quantitative-kind-family.md) records the boundary — why charts are not diagram kinds, the second scene grammar, scene element granularity, the six-slot series-colour shape, the terminal honesty ladder (exact and faithful frame tiers), the permanent braille refusal, the binding honesty rules, static-first, the bar orientation hint, and the deferred diverging-bar variant. [ADR-0032](../_adr/0032-use-the-medium-contrast-series-palette.md) records the palette values and layered identity guards that amend ADR-0030's first palette.

The surface is public: [`src/chart/mod.ts`](../../src/chart/mod.ts) is the `./chart` entrypoint, exporting authoring types, `chartAltText`, `describeChart`, kind Metadata with the generated author guide, the refused-form table (`CHART_REFUSED_FORMS`), the closed number format vocabulary, `chartSeriesLegend`, the error types, and the standalone `renderChartSvg` — while deliberately withholding `ChartScene`, the layout dispatch, and `conformChartScene`, so callers author data and never scenes. A release guard keeps that graph local, React-free, CLI-free, and permission-free, and the locale/clock ban rides the same graph. The chart family's terminal projector surface is declared in the family configuration: the generated [`chart-cli-registry.ts`](../../src/generated/chart-cli-registry.ts) maps each kind to its callable projector, and the stance pairing makes a missing projector a generation failure. Terminal frames ignore SVG-side presentation hints, and every typed decline routes to the universal description with its data section as a real aligned table.

## The kind library

The library is a closed canonical set chosen by the reader's verb; each kind's `*.meta.ts` declares its stance, honesty tier, budgets, and remedies, and the taxonomy evidence behind the set lives in the planning note the programme maintains. An `exact` frame prints every authored value inside its width envelope; a `faithful` frame is a declared-resolution approximation that prints its extremes, states its resolution, and never distorts — the lossless table is always one mode away.

| Kind           | Reader's verb                                     | Tier     | Terminal form                                                                            |
| -------------- | ------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------- |
| `bar`          | Compare magnitudes across categories or shares    | exact    | Label, eighth-block bar from a zero baseline, printed value on every row                 |
| `line`         | Follow a quantity over an ordered or date domain  | faithful | Box-drawing path, one authored point per column, printed extremes and stated resolution  |
| `distribution` | See how recorded values spread                    | exact    | Bin range, eighth-block bar, printed count per row; box summary with all five numbers    |
| `heatmap`      | Read one quantity across a grid of two categories | faithful | Shade swatches at Unicode, exact bin-index digits at ASCII, edges stated in the legend   |
| `scatter`      | Judge the relationship between two quantities     | faithful | Marker-glyph grid with collision counts, corner extremes, exact pairs in the description |
| `slope`        | Compare before → after per item                   | exact    | Permanent textual delta list: label, both values, direction triangle, exact signed delta |

Value axes on the position-encoding kinds accept the pinned base-ten log scale; length-encoding kinds keep their zero baseline, and their magnitude-span refusals name the log-scale remedy where position encoding makes it honest. Misleading forms never become kinds: the generated author guide ends with the refused forms and their remedies — pie and donut route to the bar proportion variant, gauges to the Meter Component, dual axes to two figures or common-base indexing, stacked areas to small multiples or the table, while radar, bubbles, 3D, and streaming are refused outright — rendered from [`src/chart/refusals.ts`](../../src/chart/refusals.ts) through codegen.

Sparkline is deliberately not a kind: it has no axes and no scale vocabulary, and it is lossy by design, so it ships as the Display [`Sparkline`](../../src/components/display/sparkline/) Component — a block-context movement run on the shared vertical eighth-block ramp whose mandatory endpoint annotation carries the numeric truth in every charset — and `Stat` composes it through its additive `sparkline` slot.

The Editorial `Chart` Component ([`src/components/editorial/chart/`](../../src/components/editorial/chart/)) projects the same conformant scene to live-token semantic SVG and, through `renderChartCli`, to the terminal; `DataFigure` accepts the spec-derived series legend tones in both projections, and `chartSeriesLegend` returns the empty inventory for kinds without categorical series slots.

[`MarkdownChartResource`](../../src/chart/markdown.ts) is the explicit bridge back from an ordinary generated asset reference to typed semantics, riding the shared resource-family resolver ([ADR-0031](../_adr/0031-promote-markdown-images-through-one-resource-family.md)) rather than a chart-side copy. The author workflow: author a typed spec, write `renderChartSvg(spec)` to a chosen asset path at the consumer's own build, reference it with ordinary image syntax — `renderChartMarkdownImage(resource)` serializes the safe form with `chartAltText(spec)` as alt and the summary as title — and optionally supply the same resource through `Markdown`'s and `renderMarkdownCli`'s `charts` collections or a `MarkdownBrowserDocument` to upgrade the isolated matching image to the live `Chart` or `renderChartCli`. Every registered kind flows through this bridge with no per-kind Markdown work. Because `chartAltText` derives from title and summary only, regenerating the asset from refreshed data never breaks alt equality unless the author edits the summary. Matching, normalisation, duplicate rejection (including a source admitted by both the diagram and chart collections), and drift refusal are the one shared authority in the Markdown model; no package projection reads the asset. The suite is [`tests/markdown_chart_test.tsx`](../../tests/markdown_chart_test.tsx), mirror for mirror with the diagram suite. Series colour lives in [`tokens.ts`](../../src/tokens/tokens.ts) as the documented public `--discern-color-series-1..6` family — browser-overridable custom properties, terminal-fixed by design — and [`src/chart/palette.ts`](../../src/chart/palette.ts) resolves the paint-role→Token map to literals for standalone assets, including the `ramp-1..4` sequential roles that ride the accent scale for declared-bin magnitude.

## Where a maintainer starts

Everything lives under [`src/chart/`](../../src/chart/), composed from the shared internal authorities in [`src/internal/`](../../src/internal/) — never from `src/diagram/`:

- **Numerics** — [`decimal.ts`](../../src/chart/decimal.ts) owns scaled-decimal integer arithmetic with half-away-from-zero rounding and exact subtraction; [`format.ts`](../../src/chart/format.ts) owns the closed locale-free number format vocabulary; [`value-text.ts`](../../src/chart/value-text.ts) owns the one wording every surface prints a stated value or declared gap in; [`scale.ts`](../../src/chart/scale.ts) owns the linear, band, and pinned base-ten log scales; [`ticks.ts`](../../src/chart/ticks.ts) owns Heckbert 1-2-5 tick selection in integer space, decade and subdivided log ticks, and the pinned calendar ladder over day ordinals; [`dates.ts`](../../src/chart/dates.ts) binds the shared proleptic-Gregorian grammar in [`src/internal/iso-date.ts`](../../src/internal/iso-date.ts) to the chart refusal vocabulary. A locale/clock graph guard in [`tests/chart/graph_guard_test.ts`](../../tests/chart/graph_guard_test.ts) bans `Intl`, `toLocaleString`, `toFixed`, and `Date` construction from the chart module graph.
- **Scene** — [`scene.ts`](../../src/chart/scene.ts) declares the closed grammar, including the four sequential ramp paint slots and the closed marker-shape vocabulary; [`conformance.ts`](../../src/chart/conformance.ts) proves bounds, containment, clearance, declared z-order, and no undeclared overlaps before any projection sees a scene; [`geometry.ts`](../../src/chart/geometry.ts) pins the visual grammar constants; [`kind-layout.ts`](../../src/chart/kind-layout.ts) constructs axis lines, reference lines, and anchored tick labels once for every kind layout.
- **Kinds** — each folder under [`src/chart/kinds/`](../../src/chart/kinds/) owns the fixed anatomy the family framework enforces; [`scripts/generate.ts`](../../scripts/generate.ts) supplies the chart family configuration and [`scripts/kind-family.ts`](../../scripts/kind-family.ts) generates the spec union, Metadata, registry, dispatch, export barrel, and author guide with its refused-form appendix. Budgets, stances, and honesty tiers come from each kind's `*.meta.ts`.
- **Series palette** — the authored light/dark values live only in [`tokens.ts`](../../src/tokens/tokens.ts) as `--discern-color-series-1..6`; [`tests/chart/palette_test.ts`](../../tests/chart/palette_test.ts) pins the selected references, severe protan/deutan adjacent-pair floor, ANSI 256 distinctness, and accepted ANSI 16 collapse through the real derived terminal themes, while [`tests/cli/glyph_ramps_test.ts`](../../tests/cli/glyph_ramps_test.ts) guards the non-colour identity cues.
- **Terminal glyphs** — [`src/cli/glyph-ramps.ts`](../../src/cli/glyph-ramps.ts) owns the eighth-block ramps, the shade ramp with its digit-bin ASCII alternative, the line-path vocabulary with its asterisk-with-dot ASCII idiom, the box-summary cells, the declared-gap glyph, the per-slot marker and fill tables, and the N-category proportional block allocator, each Unicode glyph paired with a one-cell ASCII fallback and enrolled in the cell-geometry guard.

The bar kind remains the founding vertical slice; read [`src/chart/kinds/bar/`](../../src/chart/kinds/bar/) end to end to see the full anatomy — spec, validation (zero baseline, negative refusal, declared gaps, proportion totals), scale-driven layout, description, and release corpus — before writing or reviewing any kind, then diff a sibling kind against it to see where a kind may lawfully differ.

## Relation to neighbours

The [diagram subsystem](../25-diagrams/README.md) shares the kind-family framework and the `src/internal/` authorities but nothing else; neither family imports the other. [Codegen](../30-codegen/README.md) documents the family framework itself. The [CLI subsystem](../70-cli/README.md) owns the text, theme, capability, and glyph authorities every chart frame composes; the chart core never imports from `src/cli/`.
