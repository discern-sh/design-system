# 2G — Clarify operational status and evidence

An operational view tells the reader what happened, what needs attention, and where to inspect supporting evidence.

**Proposal coverage:** G1, G2, G3, G4, G5. **Worktree name:** `refine-2g`. **Relative size:** L. **Programme:** design-polish.

Other streams are in flight. You own `2G` only; do not launch, dispatch, or supervise the sibling briefs.

## Orient, satisfy prerequisites, then re-root

Start with `discern_status` at `/Users/jack/Sites/discern-design-system`. If this exact effort already has a worktree, continue at its recorded path; never create a second one or adopt a sibling's idle checkout.

- 1B: Make published copy actions work in static output. Verify `map/_private/planning/design-polish/_done/1b-static-copy-contract.md` on main **and** the delivered contract described in that brief.

This is an independently landed workstream, not a below-trunk stack. The numbers in this programme are dependency tiers: only the prerequisites above block you. Do not wait for unrelated lower-key briefs. These prompts are ready for dispatch after their named prerequisites land. If dispatched early, use the `discern-await-the-fleet` skill and the predecessor's **exact returned identity and branch**, captured by the dispatching session, to await its landing. Never guess a suffixed branch from `refine-1a` or another requested name. If that identity is missing or ambiguous, report the missing dispatch record and do not start dependent implementation.

Once ready, call `discern_start` from the main checkout with the literal name `refine-2g`, then re-root all reads, edits, commands, and discern calls to its returned absolute path. Follow the await tool's start/update hint if applicable. Read the worktree's `AGENTS.md`, `map/00-orientation/design-principles.md`, and this brief there before source work. Verify every anchor against the live tree; 1C intentionally moves some Catalogue anchors.

## Background and outcome

The Workflow and Agents groups already expose results, diagnostics, work logs, fleet/activity state, transcripts, and verification reports. Their dense examples need a coherent information hierarchy; copy actions must reuse the resolved static contract.

The owner completed A1–A5 on main, with baseline commit `a1bd80f3886e`: shared control sizes, rhythm/type roles, quieter elevation, tonal hierarchy, and composed Foundation review. Preserve that work and judge current behaviour before repeating a correction. This repository is the public library and its Catalogue; the discern tool and sibling consumer projects are outside this brief.

## Read the authorities

- `src/components/workflow/`
- `src/components/agents/`
- `tests/workflow_examples_test.tsx`
- `tests/cli/workflow_summaries_test.ts`
- `tests/cli/workflow_commands_test.ts`
- `tests/cli/agents_operations_test.ts`

## Deliverables

- **G1.** Restructure Result summary, Diagnostic, and Verification report hierarchy around outcome and useful next action before secondary evidence. Preserve severity, location, and precise evidence; avoid a badge/shadow/header at every nesting level.

- **G2.** Give paths, branch names, artifact identifiers, and command context a consistent long-string policy that preserves the distinguishing part. Keep an accessible complete value and exact copy content. Do not truncate from one end indiscriminately or hide the only difference between similar artifacts.

- **G3.** Make waiting, working, blocked, and complete activity/fleet states distinguishable at a glance with visible language or named glyphs. Give blocked work an actionable explanation and reduce routine-state noise without conflating semantic states.

- **G4.** Group consecutive routine transcript/work-log entries where the existing data supports it. Preserve speaker identity, chronological meaning, decisions, boundaries, and accessibility; do not infer a hidden conversation or change records to make them prettier.

- **G5.** Use progressive disclosure for raw output and report evidence. Closed summaries should identify subject, extent, and outcome; expanding preserves keyboard reachability and precise content. Native disclosure is available, but do not implement deferred public Disclosure R2.

## Ownership and exclusions

You own:

- The Workflow and Agents Component groups, their framework-neutral local vocabularies, examples, CSS, and CLI projections.
- Existing workflow/agent tests and new focused hierarchy/disclosure regression coverage.

Out of scope: Public People Components, generic Card/Tag internals, CopyButton internals, actual discern product output, global tokens, and a new log ingestion or application state engine. All proposals deferred to `discern/TODO.md` stay deferred. Do not opportunistically implement a sibling brief.

Required companion edits are allocated to this stream: its own Unreleased changelog bullet for public contract/byte changes; its own map leaf and exact index link when needed; its own brief and programme-index row; exact enrollments in existing guards for paths/capabilities already declared above; and generated output from the owning commands. Preserve sibling entries. These routine enrollments, regenerations, conflict resolutions, and moved-link repairs need no second permission exchange. Keep new shared behaviour and contract decisions with their assigned owner; the deliverables in this brief are already authorised.

Authored feature files stay with their owner. Shared derived files and the narrow companion entries are an explicit integration exception, not disjoint authored work: `src/generated/`, `scripts/generated/`, and the author-skill evals use the configured generator/merge regeneration. The tracked `catalogue/generated/example-images-manifest.ts` and PNGs also come only from the image command and are not covered by that merge driver. Never hand-merge their facts or pixels. Coordinate the short landing turn, call `discern_update`, follow its conflict recovery, and regenerate from the complete merged authored tree. If an image conflict blocks importing its manifest, recover a parseable generator input through the printed remedy and regenerate before committing; neither side alone is a finished resolution.

## Implementation constraints

- Keep namespaced/scoped output, deterministic selected emission, React-free neutral/CLI graphs, and the build-time React contract. Native browser semantics are the default; selected behaviour must be explicit and justified.
- Use existing Appearance, control-size, type, spacing, motion, and semantic roles. Themes move tokens; do not retune the shared foundations under a local polish brief. Keep the interface-text floor, density target floor, status witnesses, forced-colour focus, and reduced-motion meaning.
- Preserve public names and canonical example identities unless an explicit, documented contract change is necessary. Generic examples belong here; product claims, routes, and bespoke consumer artwork do not.
- Use `discern-cure-a-bug` for a demonstrated defect and leave a practical regression guard at the authority that owns its class. Use `add-a-component` for new Components and `discern-write-adr` for surprising/hard-to-reverse decisions.
- Do not hand-edit generated registries, manifests, or images. Run `deno task codegen` for affected authored package facts and `deno task catalogue:images --update` when canonical Web output changes. Reuse the existing review instrument rather than inventing a screenshot gate.
- Never loosen a standard or assert an unmet checkpoint is met. Return a measured, concrete owner decision if the finished minimal design cannot satisfy a limit.

## Verification and definition of done

- Dense generic fixtures cover failure, blocked work, success, long similar paths, repeated speakers, and large evidence sets in narrow/wide browser and terminal allocations.
- The first reading pass identifies outcome and next action; expanded evidence retains exact meaning, labels, and copy values.
- Keyboard disclosure, status witnesses, CLI deterministic text, Appearance propagation, and source-derived example enrollment remain correct.

- The semantic bar is the goal at the top of this brief: demonstrate the real user task, not only isolated snapshots or passing selectors. Record what changed, why, tested widths/states, and any remaining limitation.
- Extend focused tests in the allocated area and authored conformance/postures where meaningful. Run direct tests through `discern queue -- <command>`; the repository admits one complete test run at a time. Avoid a redundant full test preflight before `discern_done`.
- After all edits and any integration update, run `discern_prepare`, commit logical changes atomically, then run `discern_done` on the clean final HEAD. Re-run on a changed HEAD; an earlier Proof does not cover new edits.
- Run the Catalogue at the worktree's deterministic `discern identity --port` port and complete the required visual review at the exact localhost URLs. Leave the preview running after `discern_done` so the project owner can review your changes. Stop the preview before running `discern_accept`, as acceptance will remove the worktree. Preview evidence: Result summary, Diagnostic, Fleet, Transcript, Verification report, and Raw output examples with dense review postures. Include `?surface=cli#component-<slug>` links for changed CLI renderers, and the live playground when an interactive adapter flow changes.
- **Landing rule chosen by the owner:** After green `discern_done`, report the exact Proof, branch, worktree, preview URL, and any owner decision, then stop and wait for the owner to confirm acceptance.
- Return the branch/worktree identity for an adversarial review against this brief. The planning agent will inspect the diff and reproduce the important journeys; gate success alone is not a visual review.
- In the final implementation commit, before the final prepare → commit → done sequence, move `map/_private/planning/design-polish/2g-operational-hierarchy-and-evidence.md` to `map/_private/planning/design-polish/_done/2g-operational-hierarchy-and-evidence.md`, repair any moved relative links, and update **only this stream's row** in the programme README to the `_done/` path and completed state, preserving every sibling row.

## Delivered evidence — 2026-09-13

Implementation is complete in `agent/refine-2g-74d96d`, at `/Users/jack/Sites/discern-design-system.worktrees/refine-2g-74d96d`. Acceptance remains an owner decision after the final exact-HEAD Proof. The [operational evidence contract](../../../../20-components/operational-evidence.md) records the supported API and its boundaries.

- G1/G5: Result summary and Diagnostic put the useful next action before readings, location and evidence. Verification report puts its outcome, summary and next action before a native disclosure containing every metadata field and check. Raw output names its subject, authored-line extent and caller-reported outcome. Native controls keep exact browser evidence reachable by keyboard.
- G2: Workflow and Agents identity text wraps in browser allocations. Terminal path rows preserve the complete value; compact Fleet detects otherwise indistinguishable branch cells and prints full continuations. Copy actions use 1B's selected static runtime without another clipboard implementation.
- G3: Fleet and Activity log expose Working, Waiting, Blocked and Complete with visible words. Blocked fixtures explain the missing prerequisite and how to recover. Lifecycle and semantic outcomes remain distinct at every terminal capability level.
- G4: Explicit routine annotations group adjacent compatible entries. All original list items, accessible speaker identities, timestamps and content remain. An unmarked decision, speaker change, failed step or active step remains a boundary.

### User journeys and review

The first pass through the failed result and report identifies the two failed fixtures and the corrective action. Opening the report exposes 24 checks: 21 passed, two failed and one skipped, including both similar alpha/beta paths and their distinct missing fields. The Fleet view distinguishes waiting for review from blocked on a missing fixture. Transcript and Worklog fixtures show three routine reads, the separate decision and the following reviewer/failure boundary.

Reviewed the Catalogue's existing instrument at 390 and 1120 CSS-pixel allocations with light/dark appearances. Fleet additionally uses Accent 255, Structure 0.35 and Density 0.8. The focused browser journey covers both widths and poles, forced-colour focus, reduced motion, full long paths (including Windows and an unsplit artifact ID), Enter/Tab disclosure, every original record and exact whitespace/CRLF/Unicode copy content. Its static fixture uses emitted Workflow/Agents CSS and the selected CopyButton behaviour.

The focused queued suite passed 65 tests. Terminal checks cover dense frames at 24/60/100 columns, Unicode/ASCII, no colour and all three colour depths; existing narrow exact frames cover 16/20 columns. The live `deno task playground:cli activity-log` journey completed in an 80×24 terminal both with ANSI control and append-only degradation, retaining pinned results and the compact completion result.

The reel uses click/focus actions to present native open states; its synthetic key dispatch cannot trigger browser default disclosure. Real keyboard semantics are exercised by browser tests and manual Enter/Tab inspection. Canonical images come from `deno task catalogue:images --update`; generated package projections come from `deno task codegen`.

Exact review entry points (the authored posture determines the allocation):

- [Result summary, closed wide](http://localhost:18841/catalogue/reviews/components/?group=Workflow&component=result-summary&example=failed&posture=failed-wide&width=wide&theme=light&mode=reel) and [open narrow](http://localhost:18841/catalogue/reviews/components/?group=Workflow&component=result-summary&example=failed&posture=result-evidence&width=narrow&theme=dark&mode=reel).
- [Diagnostic evidence, narrow](http://localhost:18841/catalogue/reviews/components/?group=Workflow&component=diagnostic&example=verbose-failure&posture=diagnostic-evidence&width=narrow&theme=light&mode=reel).
- [Fleet, narrow](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=fleet&example=dense&posture=dense-narrow&width=narrow&theme=dark&mode=reel) and [wide with Appearance](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=fleet&example=dense&posture=dense-wide&width=wide&theme=light&accent=255&field=0%2C0.35%2C1%2C0.8&mode=reel).
- [Transcript, narrow](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=transcript&example=dense&posture=dense-narrow&width=narrow&theme=light&mode=reel) and [wide](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=transcript&example=dense&posture=dense-wide&width=wide&theme=dark&mode=reel).
- [Verification report, closed wide](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=verification-report&example=dense&posture=dense-wide&width=wide&theme=dark&mode=reel) and [all evidence, narrow](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=verification-report&example=dense&posture=open-evidence&width=narrow&theme=light&mode=reel).
- [Raw output, narrow](http://localhost:18841/catalogue/reviews/components/?group=Workflow&component=raw-output&example=expanded&posture=raw-narrow&width=narrow&theme=dark&mode=reel) and [wide](http://localhost:18841/catalogue/reviews/components/?group=Workflow&component=raw-output&example=expanded&posture=raw-wide&width=wide&theme=light&mode=reel).
- [Worklog routine boundaries](http://localhost:18841/catalogue/reviews/components/?group=Agents&component=worklog&example=dense&posture=dense-narrow&width=narrow&theme=light&mode=reel).

Changed terminal renderers: [Result summary](http://localhost:18841/?surface=cli#component-result-summary), [Diagnostic](http://localhost:18841/?surface=cli#component-diagnostic), [Verification report](http://localhost:18841/?surface=cli#component-verification-report), [Raw output](http://localhost:18841/?surface=cli#component-raw-output), [Fleet](http://localhost:18841/?surface=cli#component-fleet), [Transcript](http://localhost:18841/?surface=cli#component-transcript), [Worklog](http://localhost:18841/?surface=cli#component-worklog), [Activity log](http://localhost:18841/?surface=cli#component-activity-log), [Path reference](http://localhost:18841/?surface=cli#component-path-reference), [Command](http://localhost:18841/?surface=cli#component-command), [File change](http://localhost:18841/?surface=cli#component-file-change), [Artifact card](http://localhost:18841/?surface=cli#component-artifact-card), and [Artifact tree](http://localhost:18841/?surface=cli#component-artifact-tree).

### Boundaries

Raw browser output retains source whitespace through native scrolling. Terminal evidence reflows to the allocation, so the browser's source/copy surface remains the exact formatting authority. Fleet's existing lossless mode permits overflow to retain contiguous identity; compact mode retains its compatibility contract while exposing ambiguous branches. Routine grouping requires explicit caller annotations and rich speaker identity; it does not infer hidden conversations or decisions. No global tokens, public Disclosure Component, application state engine or sibling consumer output changes are included.
